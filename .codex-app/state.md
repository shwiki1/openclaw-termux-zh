# Current App State

Last updated: 2026-07-21 UTC

## Current Truth
- App: `次元虾` / package `com.agent.cyx`. Flutter Android shell + Kotlin native services + PRoot Ubuntu RootFS + legacy Node CLI.
- Stack: Flutter/Dart Android shell, Kotlin native Android services, PRoot Ubuntu RootFS runtime, legacy Node.js CLI package.
- Cloud build: `.github/workflows/flutter-build.yml` builds arm64-v8a APK, requires a reusable prebuilt RootFS by default, runs `flutter analyze --no-fatal-infos`, uploads the APK as GitHub artifact `ciyuanxia-apks`, and can publish a GitHub Release; it does not run `flutter test`. The Gitee split-parts upload step was removed locally on 2026-07-18 after repeated timeout risk, and default RootFS fallback rebuild was disabled after run `29651050217` hit a prebuilt fingerprint mismatch.
- App version: published display `5.4`; latest GitHub artifact cloud candidate display `8.5`; source semantic anchor remains `2.5.0`.
- Build number: published logical build `173`; latest GitHub artifact cloud candidate build `204`; next fresh cloud build must be greater than `204`.
- Repository root: `/storage/emulated/0/ZeroTermux/开发/openclaw-termux-zh-5.5`.
- Source version anchor: root `package.json` `2.5.0`; Flutter `pubspec.yaml` `2.5.0+143`.
- Active local branch: `codex-terminal-ime-lag-fix`; latest confirmed GitHub feature-branch build head is remote SHA `c8a87a3384edf27350e7f4125f679363ec0d87b0` for the 2026-07-21 cleanup validation build `8.5 / 204`. Earlier remote SHA `ff7c6f6299ac2845a9af01e21b49fd0599fde196` is the previous `8.4 / 203` CodeBuddy local model alias build.
- Remotes: `origin` Gitee, `shwiki` GitHub.
- Latest published GitHub Release: `v5.4.0 / 5.4 / 173` from Actions `29538124523`.
- Latest GitHub artifact cloud candidate: `8.5 / 204` from Actions `29867482912` on branch `codex-terminal-ime-lag-fix` at remote SHA `c8a87a3384edf27350e7f4125f679363ec0d87b0`; APK `CiYuanXia-v8.5-204-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8509966202`, artifact digest `sha256:0efb1d1438808a284c6591a2a1835cf809426006cbd72db13671c9f7764074b3`, final artifact size `311437095` bytes. RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `8.5 / 204` artifact: direct artifact API download saved `dist/github-run-29867482912/ciyuanxia-apks.zip` with SHA-256 `0efb1d1438808a284c6591a2a1835cf809426006cbd72db13671c9f7764074b3`, matching GitHub artifact digest; `unzip -t` passed and extracted `dist/github-run-29867482912/CiYuanXia-v8.5-204-arm64-v8a.apk`, size `325175770` bytes, APK SHA-256 `42d29e264f4a92b4829121b4ac9c79f5465596fd22240e6826e9f7810f336675`. APK inspection confirmed api2py frontend/backend assets, `data/config.example.json`, offline Tailwind/Lucide assets, and `openclaw-rootfs-noble-arm64.tar.gz` are present.
- Previous GitHub artifact cloud candidate: `8.4 / 203` from Actions `29740833706` on branch `codex-terminal-ime-lag-fix` at remote SHA `ff7c6f6299ac2845a9af01e21b49fd0599fde196`; APK `CiYuanXia-v8.4-203-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8460430702`, artifact digest `sha256:878cd528467a07825a95270148b7dbf7ef8805537ae3bac7bb4991875febdecd`, final artifact size `311436689` bytes. RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `8.4 / 203` artifact: direct artifact API download extracted `dist/github-run-29740833706/CiYuanXia-v8.4-203-arm64-v8a.apk`, size `325175564` bytes, APK SHA-256 `138036ffcfe0a740d8f2dc0785c592ded3fdec39cbeb2e8c7191cbdf6f7dbf36`. ZIP SHA-256 `878cd528467a07825a95270148b7dbf7ef8805537ae3bac7bb4991875febdecd` matches GitHub artifact digest, `unzip -t` passed, and APK inspection confirmed api2py frontend/backend assets, `data/config.example.json`, offline Tailwind/Lucide assets, and `openclaw-rootfs-noble-arm64.tar.gz` are present.
- Previous GitHub artifact cloud candidate: `8.2 / 201` from Actions `29716934002` on branch `codex-terminal-ime-lag-fix` at remote SHA `d0f85aa86de49ecda1bf8060976f7502dbf165da`; APK `CiYuanXia-v8.2-201-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8451064233`, artifact digest `sha256:9d013f65db1a4299709fd6426c68ecef3c929bd378a401d2798fc23d73ab07d7`, final artifact size `311436284` bytes. RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `8.2 / 201` artifact: `dist/github-run-29716934002/ciyuanxia-apks.zip` SHA-256 `9d013f65db1a4299709fd6426c68ecef3c929bd378a401d2798fc23d73ab07d7` matches GitHub artifact digest; `unzip -t` passed. Extracted APK is `dist/github-run-29716934002/CiYuanXia-v8.2-201-arm64-v8a.apk`, size `325175094` bytes, APK SHA-256 `b02f4743aac2c98fc9a8e01cec872516e73e4deb78dfa98a946842d8a8d3de40`. APK inspection confirmed `assets/flutter_assets/assets/api2py/app/config.py`, `assets/flutter_assets/assets/api2py/app/main.py`, `assets/flutter_assets/assets/api2py/data/config.example.json`, `assets/flutter_assets/assets/api2py/public/static/index.html`, `assets/flutter_assets/assets/api2py/public/static/lib/lucide.js`, `assets/flutter_assets/assets/api2py/public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` are present.
- Previous GitHub artifact cloud candidate: `8.1 / 200` from Actions `29708374513` on branch `codex-terminal-ime-lag-fix` at remote SHA `ed9bcafebd55b73509d814e5fa151c230919acb7`; APK `CiYuanXia-v8.1-200-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8449141517`, artifact digest `sha256:1681bc77a49a47756d9f1951cf646689af80fe3cc83ef83dfaf797cceac01336`, final artifact size `311434614` bytes. The run was delayed by a GitHub Actions/API outage, then completed successfully; prebuilt RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `8.1 / 200` artifact: `dist/github-run-29708374513/ciyuanxia-apks.zip` SHA-256 `1681bc77a49a47756d9f1951cf646689af80fe3cc83ef83dfaf797cceac01336` matches GitHub artifact digest; `unzip -t` passed. Extracted APK is `dist/github-run-29708374513/CiYuanXia-v8.1-200-arm64-v8a.apk`, size `325173670` bytes, APK SHA-256 `039f418d1a54566b639e69474a4fb72ea24a7322a1088f28132c90bf8a60c83e`. APK inspection confirmed `assets/flutter_assets/assets/api2py/app/config.py`, `assets/flutter_assets/assets/api2py/app/main.py`, `assets/flutter_assets/assets/api2py/data/config.example.json`, `assets/flutter_assets/assets/api2py/public/static/index.html`, `assets/flutter_assets/assets/api2py/public/static/lib/lucide.js`, `assets/flutter_assets/assets/api2py/public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` are present.
- Previous GitHub artifact cloud candidate: `7.9 / 198` from Actions `29685551102` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `ab6c17678e5bcf3cb6a84f59bf18cc5890d70b10`; APK `CiYuanXia-v7.9-198-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8442004536`, artifact digest `sha256:64f9d2cd5ffc749957740a7f8ebdbcfd2078de3b5cdfc23e6ac64c389d007eea`, final artifact size `311433807` bytes. This build was triggered by the push for `fix: polish local proxy browser mapping`; prebuilt RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `7.9 / 198` artifact: `dist/github-run-29685551102/ciyuanxia-apks.zip` SHA-256 `64f9d2cd5ffc749957740a7f8ebdbcfd2078de3b5cdfc23e6ac64c389d007eea` matches GitHub artifact digest; `unzip -t` passed. Extracted APK is `dist/github-run-29685551102/CiYuanXia-v7.9-198-arm64-v8a.apk`, size `325171698` bytes, APK SHA-256 `fd93c76054d03221d12380acd04b74cf702007cc58ea81203be1597c398a40b1`. APK inspection confirmed `assets/flutter_assets/assets/api2py/app/main.py`, `assets/flutter_assets/assets/api2py/public/static/index.html`, `assets/flutter_assets/assets/api2py/public/static/lib/lucide.js`, `assets/flutter_assets/assets/api2py/public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` are present.
- Previous GitHub artifact cloud candidate: `7.7 / 196` from Actions `29678484884` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `660cdc94373a3b2c27e798763043699ac5f174c0`; APK `CiYuanXia-v7.7-196-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8439821802`, artifact digest `sha256:4ac2a75d0b389d6a3c827193ff83c7351f69ff7550bceb4f1d2c04437a40a9e4`, final artifact size `311419885` bytes. This was a normal `workflow_dispatch` with `rebuild_rootfs=false`: prebuilt RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Latest GitHub artifact cloud candidate: `7.8 / 197` from Actions `29682658382` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `4d921ebba572e903eefb2e44706de91d3e04d553`; APK `CiYuanXia-v7.8-197-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8441127491`, artifact digest `sha256:5e7427781beea17daa66c6f9dd552e30593a942ea890c9f0fa8ee274a2954f83`, final artifact size `311424821` bytes. This build was triggered by the push for `fix: prevent local proxy recursion`; prebuilt RootFS restore passed from `basic-resource`, `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped, `Verify bundled OpenClaw rootfs`, Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. A duplicate manual workflow run `29682671703` was cancelled to avoid two APKs racing build numbers. No Gitee upload step ran.
- Local downloaded `7.8 / 197` artifact: `dist/github-run-29682658382/ciyuanxia-apks.zip` SHA-256 `5e7427781beea17daa66c6f9dd552e30593a942ea890c9f0fa8ee274a2954f83` matches GitHub artifact digest; `unzip -t` passed. Extracted APK is `dist/github-run-29682658382/CiYuanXia-v7.8-197-arm64-v8a.apk`, size `311M`, APK SHA-256 `885bfb8ee08d7fa481c37d46b2a919a283c89240e73a3b41059786666d687e9f`. APK inspection confirmed `assets/flutter_assets/assets/api2py/app/config.py`, `assets/flutter_assets/assets/api2py/public/static/index.html`, `assets/flutter_assets/assets/api2py/public/static/lib/lucide.js`, `assets/flutter_assets/assets/api2py/public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` are present.
- Local downloaded `7.7 / 196` artifact: `dist/github-run-29678484884/ciyuanxia-apks.zip` SHA-256 `4ac2a75d0b389d6a3c827193ff83c7351f69ff7550bceb4f1d2c04437a40a9e4` matches GitHub artifact digest; `unzip -t` passed. Extracted APK is `dist/github-run-29678484884/CiYuanXia-v7.7-196-arm64-v8a.apk`, size `311M`, APK SHA-256 `c7a306d12ea990a53e92c071e0f748f4a40a429af145c6123c92536b5eb5de38`. APK inspection confirmed `assets/flutter_assets/assets/api2py/server.py`, `assets/flutter_assets/assets/api2py/app/config.py`, `assets/flutter_assets/assets/api2py/public/static/index.html`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` are present.
- Superseded duplicate build: Actions run `29677549463` succeeded at remote SHA `8cd3ef78051b96b87e75d5a552721775fdbaca49` but produced `CiYuanXia-v7.6-195-arm64-v8a.apk` again because the workflow only inspected recent `push` runs and missed manual `workflow_dispatch` build `29674538930`. The workflow was fixed in commit `660cdc94373a3b2c27e798763043699ac5f174c0` to include all completed runs, not only push events, before producing the valid `7.7 / 196` artifact.
- Local downloaded `7.6 / 195` artifact: `dist/github-run-29674538930/ciyuanxia-apks.zip` matches artifact digest `sha256:ab4381a0f2529f6e771f0953ce2aab3cb1b140dc5e35ce8afcccdde785c07a8a`; `unzip -t` passed. Extracted APK is `dist/github-run-29674538930/CiYuanXia-v7.6-195-arm64-v8a.apk`, size `311M`, APK SHA-256 `87a62f9098b6732051ed08ed0a309e130abcc7954dde43e671b1a48c24001660`. APK inspection confirmed api2py/backend/frontend assets are present plus the rebuilt RootFS: `assets/flutter_assets/assets/api2py/server.py`, `requirements.txt`, `app/config.py`, `app/main.py`, `public/static/index.html`, `public/static/lib/lucide.js`, `public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz`.
- Latest `basic-resource` release assets after run `29674538930`: `openclaw-rootfs-noble-arm64.tar.gz` size `279574832`, digest `sha256:a5793035aec8491f68782ddefeeabd66c643ba5ddb0e4295b2de9a65dfe37e3d`; manifest `openclaw-rootfs-noble-arm64.json` digest `sha256:1fddb20e3e8794d4e3bd2775e96a7f277ab57cd4652e57ec5b482a1800940d62`; Ubuntu base asset digest `sha256:7b2dced6dd56ad5e4a813fa25c8de307b655fdabc6ea9213175a92c48dabb048`.
- Previous GitHub artifact cloud candidate: `7.5 / 194` from Actions `29672617188` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `a9c34535bb03c889d31e0f11a167cf9bc8d6b2cb`; APK `CiYuanXia-v7.5-194-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8437839784`, artifact digest `sha256:1f607c9ab3f2b7782e6e967d8c5e28c9fd626c04b218317392a6de8f0dda13aa`, final artifact size `303222620` bytes. RootFS was reused and verified from the published `basic-resource` archive; `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped. Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `7.5 / 194` artifact: `dist/github-run-29672617188/ciyuanxia-apks.zip` matches artifact digest `sha256:1f607c9ab3f2b7782e6e967d8c5e28c9fd626c04b218317392a6de8f0dda13aa`; `unzip -t` passed. Extracted APK is `dist/github-run-29672617188/CiYuanXia-v7.5-194-arm64-v8a.apk` with APK SHA-256 `312fba0adb69281c6589854dab65b3c72f7b13d0803238ea82c93b96e6779198`. APK inspection confirmed api2py backend/frontend assets are present: `assets/flutter_assets/assets/api2py/app/config.py`, `app/main.py`, `public/static/index.html`, `public/static/lib/lucide.js`, and `public/static/lib/tailwind.js`.
- Latest GitHub artifact cloud candidate: `7.3 / 192` from Actions `29659937270` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `07c35b49aa8bf71fbffc22238675d63ab5b574e4`; APK `CiYuanXia-v7.3-192-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8434060052`, artifact digest `sha256:28e1a6af64566fe7d972ef956ca9c20caba6e244b1dc73036ca4988ec80178af`, final artifact size `302917204` bytes. RootFS was reused and verified from the published `basic-resource` archive; `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped. Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed. No Gitee upload step ran.
- Local downloaded `7.3 / 192` artifact: `dist/github-run-29659937270/ciyuanxia-apks.zip` matches artifact digest `sha256:28e1a6af64566fe7d972ef956ca9c20caba6e244b1dc73036ca4988ec80178af`; `unzip -t` passed. Extracted APK is `dist/github-run-29659937270/CiYuanXia-v7.3-192-arm64-v8a.apk` with size `316648741` bytes and APK SHA-256 `a1306b88b23cd60d341fbade3e03d6cd48404010d2bc7abe3f63fba367461503`.
- Latest GitHub artifact cloud candidate: `7.0 / 189` from Actions `29656506917` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `34cfcaa437c83abe838c3b4b41c64b7e37b7f733`; APK `CiYuanXia-v7.0-189-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8433086032`, artifact digest `sha256:f3fe2ae0816fb3f6fa62ebcbc5bf692952db1e926482584ad3c638287a7186ac`, final artifact size `302895358` bytes. RootFS was reused and verified; `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped. Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed.
- Latest GitHub artifact cloud candidate: `7.2 / 191` from Actions `29658373259` on branch `codex-terminal-ime-lag-fix` at API-created remote SHA `42a029c782aa043360a48547a7dfdbbb5c89596e`; APK `CiYuanXia-v7.2-191-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8433612396`, artifact digest `sha256:dfcc0cd6fe50573134f95583f2022d91ca7a962fa2d681102d79f1b0e67d3213`, final artifact size `302908398` bytes. RootFS was reused and verified; `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped. Flutter analyze, arm64 APK packaging, PRoot native-library verification, artifact collection, and GitHub artifact upload passed.
- Local downloaded `7.2 / 191` artifact: `dist/github-run-29658373259/ciyuanxia-apks.zip` matches artifact digest `sha256:dfcc0cd6fe50573134f95583f2022d91ca7a962fa2d681102d79f1b0e67d3213`; `unzip -t` passed. Extracted APK is `dist/github-run-29658373259/CiYuanXia-v7.2-191-arm64-v8a.apk` with size `316636375` bytes and APK SHA-256 `10bfcd7ca6c5de5de2e6562b7455cb45a474bbd2a8d9114e02c26bad75a68286`.
- Local downloaded `7.0 / 189` artifact: `dist/github-run-29656506917/ciyuanxia-apks.zip` matches artifact digest `sha256:f3fe2ae0816fb3f6fa62ebcbc5bf692952db1e926482584ad3c638287a7186ac`; `unzip -t` passed. Extracted APK is `dist/github-run-29656506917/CiYuanXia-v7.0-189-arm64-v8a.apk` with size `316617342` bytes and APK SHA-256 `12b54801441202d9395a96b63b60abe2452969d82ca4bba2a5cdb6dfcf78a397`.
- Local downloaded `6.8 / 187` artifact: `dist/github-run-29647690716/ciyuanxia-apks.zip` matches artifact digest `sha256:189c6eaaee4c0af6d48c54bacc0bfbeb13fcbfbba1af7ec644426a99f42f6abc`; extracted APK is `dist/github-run-29647690716/CiYuanXia-v6.8-187-arm64-v8a.apk` with APK SHA-256 `df8144fa887bc8648684a5d0105e5b2be0ded157adfd0e8551b7b5213e0105c3`.
- Latest fully Gitee-transferred cloud candidate remains `6.6 / 185` from Actions `29640675284` on branch `codex-gitee-transfer-timeout-186` at `b08f9931e715f839dc1e63401219a7faeb48699a`; APK `CiYuanXia-v6.6-185-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8428690154`, artifact digest `sha256:c4344f874ef2a64958c570eda23690b693444361e0d8c55aedc07d8f0889517c`. User reported this version is already downloaded and installed locally; no local redownload was performed in this session.
- Current APK delivery path: GitHub Actions artifact `ciyuanxia-apks` -> local download under `dist/github-run-<run-id>/` -> ZIP/APK SHA verification. Do not run the Gitee split-parts upload in the main APK workflow unless explicitly restored later.
- Terminal architecture: ordinary CLI tools open `NativeTerminalActivity`; Codex opens `NativeTerminalPagerActivity` with native browser page.
- Current packaged feature state: local `api2py` relay integration, 9999 CLI API routing, direct relay management page entry, native browser icon/UI fixes, GitHub-artifact-only delivery, and RootFS reuse correction are packaged in GitHub artifact candidate `7.0 / 189` and downloaded locally under `dist/github-run-29656506917/`.
- Current packaged feature state: proxy API management UI refinements, default local proxy key alignment, protocol-switching model fetch, bounded api2py route table height, proxy browser IME/layout polish, API access card redesign, proxy auth-token plaintext display, CLI proxy protocol ownership hotfix, Settings battery-optimization callback retry, and CodeBuddy local model alias compatibility are packaged in GitHub artifact candidate `8.4 / 203` and downloaded locally under `dist/github-run-29740833706/`.

## Active Task
- 2026-07-21 deep unused-code cleanup + cloud validation: removed tracked obsolete Gitee APK distribution scripts, added workflow concurrency/test guards, pushed cleanup commit `c8a87a3384edf27350e7f4125f679363ec0d87b0`, and validated it with successful GitHub Actions run `29867482912` producing `8.5 / 204`.
- 2026-07-21 unused local-file cleanup: remove only high-confidence unused/generated local files. Do not remove dynamically loaded Flutter assets, release notes, RootFS fallback archives, manual scripts, or Kotlin/Dart files based only on text-reference counts.
- Local-only API management replacement pass: `api2py` is bundled as the sole local API relay, opens its management UI directly without first-open admin initialization/login for local requests, and all managed CLI API saves/configs route through `http://127.0.0.1:9999/v1`. Work only in the main project directory; do not create branches/worktrees or cloud-build unless the user asks.

## Recently Changed
- 2026-07-21 deep unused-code cleanup: removed tracked obsolete Gitee distribution scripts `scripts/mirror-apk-to-gitee.py` and `scripts/upload-apk-parts-to-gitee-branch.sh`. The current workflow already publishes APKs through GitHub artifacts only, `lib/test.js` already guarded against workflow references to the split-upload script, and `.codex-app/build.md` records Gitee Release attachment limits as non-viable for current APK size. Added stronger `lib/test.js` guards that both deprecated script files must stay absent. Added `.github/workflows/flutter-build.yml` concurrency (`${{ github.workflow }}-${{ github.ref }}`, cancel in progress) to reduce same-branch build/version races before the cloud validation build.
- 2026-07-21 unused local-file cleanup: removed ignored Python bytecode caches generated under `flutter_app/assets/api2py/` and `scripts/` during local Python checks, and removed ignored local bootstrap cache `flutter_app/assets/bootstrap/claude-code-2.1.148-bundle.tar.gz` after confirming it is not tracked by Git, is covered by `.gitignore`, is not declared in `flutter_app/pubspec.yaml`, and has no source references. Kept `NativeUiStyle.kt` because its palette/helpers are referenced by native terminal/browser classes; kept sample config JSON files because `BundledSampleConfigService` loads `assets/sample_configs/openclaw` dynamically; kept RootFS/Ubuntu/Node fallback archives and scripts because they are build/runtime/manual tooling.
- 2026-07-20 CLI proxy protocol ownership hotfix: user reported that after Codex CLI switched model protocol once, other CLI protocol changes no longer took effect, and any protocol change made in the api2py backend was reverted after opening a CLI terminal. Root cause was `_mergeLocalApiProxyConfig()` treating each tool's stale `apiProtocol` as authoritative during every runtime regeneration, including terminal launch. The merge now preserves an existing proxy-side mapping `protocol` first and uses tool `apiProtocol` only to initialize a brand-new mapping; the CLI dialog loads proxy-side mapping protocol for any selected alias; explicit dropdown changes call `updateLocalApiProxyModelProtocol()` directly; and saving a tool bound to the built-in local proxy clears stale `apiProtocol` from tool settings.
- 2026-07-20 Settings battery optimization callback retry fix: the first return-refresh attempt still missed some Android devices because `requestBatteryOptimization()` returns immediately after `startActivity()`, so the old short polling could finish while the user was still in Android settings and the single `resumed` refresh could run before PowerManager updated. Settings now marks `_waitingBatteryOptimizationReturn`, restarts the battery-status polling when the app resumes, and uses a tokened delayed retry schedule through 4 seconds so late PowerManager updates repaint the row without requiring a second tap.
- 2026-07-20 CLI model protocol switching fix: separated the CLI dialog's `获取模型协议` from api2py provider upstream protocol ownership. For the built-in `本地中转代理` profile, opening the CLI config now reads the current proxy-side model mapping protocol from `/root/.openclaw/api2py/data/config.json`; saving without touching the protocol no longer writes a fixed `apiProtocol` into tool settings or overwrites proxy provider/mapping protocol. If the user explicitly changes `获取模型协议`, the app updates only that model alias mapping protocol in api2py, while preserving existing provider `type` chosen in the proxy backend. For non-local shared APIs, explicit CLI protocol changes still update the tool's mapping protocol, so non-Codex CLIs can switch their model mapping protocol instead of being ignored.
- 2026-07-19 settings battery-optimization return refresh: fixed the top-right Settings battery optimization row so returning from Android battery settings refreshes `_batteryOptimized` immediately instead of requiring a second tap. `didChangeAppLifecycleState(resumed)` now refreshes battery optimization along with storage/overlay/floating-file-manager state, and the tap flow polls `isBatteryOptimized()` briefly after returning to cover Android's delayed persistence.
- 2026-07-19 local proxy auth-token visibility follow-up: api2py settings already used textarea controls for `认证 Token`, but `/api/config` redacted `auth_tokens` before returning config to the management UI. `redact_config()` now returns `auth_tokens` in plaintext so the settings page can display and edit them directly, while `admin_tokens` and provider API keys remain masked.
- 2026-07-19 local proxy API config follow-up: fixed the per-tool `获取模型协议` dropdown so saving a CLI config persists `apiProtocol` instead of only using the selected protocol for one model-fetch request. The `API 接入` card no longer shows the old shared-API count chip, and the `中转代理` dialog now shows a fuller proxy usage guide with Base URL, default key `sk-123`, health URL, RootFS path, auto-start/restart behavior, provider/mapping ownership, local-proxy recursion avoidance, and supported protocol notes.
- 2026-07-19 local proxy mapping/browser follow-up: original `管理 API` model selection now reads richer `/v1/models` metadata from the local api2py relay, including upstream/provider name, upstream base URL, upstream model, and protocol. Saving CLI API settings now preserves existing proxy-side `model_mappings` and merges only the selected tool alias instead of deleting prior app-managed aliases, so manually added proxy mappings remain intact. The api2py `/v1/models` response now exposes `provider_base_url` and `upstream_model`, and the bundled management page/model dropdown displays those fields while using a local serif stack. `LocalApiProxyBrowserScreen` is visually aligned to the proxy page dark background, keeps the address bar at the top with proxy status on the top right, moves back/forward/refresh controls to a bottom toolbar, uses Android hybrid composition, and acquires the browser soft-input mode separately from Codex automation browser compensation.
- 2026-07-19 local proxy recursion/OpenClaw prebuilt reuse fix: original `管理 API` now exposes a protected built-in `本地中转代理` shared API profile pointing at `http://127.0.0.1:9999/v1`, but this profile is not persisted into user shared profiles and is filtered out when syncing providers into `/root/.openclaw/api2py/data/config.json`. Any shared/tool config whose base URL normalizes to `http://127.0.0.1:9999` or `/v1` is skipped as an upstream provider to prevent recursive proxying. Original API protocol choices now match api2py's supported protocols (`openai`, `responses`, `anthropic`, `ollama`), with Ollama model discovery using `/api/tags`. Bootstrap now reads `/etc/openclaw-prebuilt-rootfs` and reuses prebuilt OpenClaw from the extracted RootFS instead of downloading/reinstalling just because the selected UI release differs. The api2py management page was lightened for Android WebView by removing remote Google Fonts/serif font forcing, disabling costly transitions on small screens, debouncing search re-renders, and batching Lucide icon replacement through one animation-frame scheduler.
- 2026-07-19 local proxy page and Codex launcher hotfix: user reported the local proxy management page showed WebView page-open failure even after `重启代理` succeeded, and installed Codex failed with `/root/.openclaw/bin/codex: 23: /root/.config: Permission denied` plus ChatGPT account auth `plan type is required`. The bundled api2py management page referenced `/static/lib/tailwind.js` and `/static/lib/lucide.js`, but these local JS assets were not bundled; copied them from the user's source `api2py/public/static/lib/` into `flutter_app/assets/api2py/public/static/lib/`, declared that subdirectory in `pubspec.yaml`, expanded `LocalApiProxyService` required-file and startup checks to verify `/`, Tailwind, and Lucide, and added a typed `LocalApiProxyStatus`. `LocalApiProxyBrowserScreen` now starts/checks the proxy before loading the WebView and shows service errors in-app. The `中转代理` dialog now auto-checks and displays proxy status with a refresh button. Codex managed launcher generation now uses a single-line `mkdir -p` command to avoid `/root/.config` being executed as a command, and Codex local-proxy configs write/use `OPENAI_API_KEY=openclaw-local-proxy` when routed through `http://127.0.0.1:9999/v1` without a direct per-tool key, forcing Codex into API-key auth instead of ChatGPT account bootstrap.
- 2026-07-19 persistent local proxy/Codex default config fix: user reported installed `7.5 / 194` still showed `代理未响应 ... Connection refused ... 127.0.0.1:9999/api/health` after restart success, and Codex still failed with `/root/.config: Permission denied` plus ChatGPT account `plan type is required`. Root cause was starting api2py through one-shot PRoot with `--kill-on-exit`, which killed the background Python server when the command returned. Added `LocalApiProxyForegroundService` as a persistent Android foreground service that runs api2py in a foreground PRoot process with `exec python3 server.py`, added Flutter/native bridge methods to start/stop/check it, and changed restart/status flows to use the persistent native service after verifying bundled files/dependencies. Codex launcher generation now matches the current Termux guide/config shape: single-line directory creation, `auth.json` as `{ "OPENAI_API_KEY": "..." }`, `preferred_auth_method = "apikey"`, `sandbox_mode = "danger-full-access"`, `approval_policy = "never"`, and local proxy base URL `http://127.0.0.1:9999/v1`. Local commit is `43c5a9b`; GitHub API-created remote SHA is `5bfb21243c9e9397b26bcf790a68a11aba9b3d6f`.
- 2026-07-18 api2py missing package hotfix: user reported `7.3 / 192` still failed after pip installed dependencies, with `ModuleNotFoundError: No module named 'app'`. Local APK inspection confirmed `CiYuanXia-v7.3-192-arm64-v8a.apk` only packaged top-level `assets/api2py/*` files and omitted nested `assets/api2py/app/`, `data/`, `public/static/`, and `scripts/` files. `flutter_app/pubspec.yaml` now explicitly declares each api2py subdirectory, `LocalApiProxyService.installBundledFiles()` verifies required synced files including `app/config.py` and `public/static/index.html`, and `start.sh` fails early with a clear incomplete-resource error if the `app/` Python package is missing. `scripts/build-prebuilt-rootfs.sh` now includes `python3-pip` and preinstalls api2py Python dependencies from `flutter_app/assets/api2py/requirements.txt` when the prebuilt RootFS is intentionally rebuilt; ordinary APK builds still reuse the current published RootFS unless `BUILD_BUNDLED_ROOTFS=true` is explicitly set for a resource rebuild.
- 2026-07-18 local API relay integration: copied the user's own `api2py` lightweight Python relay into `flutter_app/assets/api2py/` excluding runtime logs, pid files, SQLite DBs, sessions, caches, and pyc files; `data/config.json` was sanitized into `data/config.example.json` with no admin account or password hash. Added `LocalApiProxyService` to sync bundled files into `/root/.openclaw/api2py`, create `data/config.json` from the template when missing, install Python dependencies if missing, start the service at `http://127.0.0.1:9999/`, and explicitly restart it from the UI. `SplashScreen` now starts the relay automatically in the background after RootFS setup is complete. Added `LocalApiProxyBrowserScreen`, a dedicated WebView manager with address bar, back, forward, and refresh controls. `api2py` local management/API requests bypass admin auth and its frontend boots directly into the app page instead of showing setup/login. The CLI tools API card presents `API 接入` with legacy `管理 API` retained and a separate `中转代理` button below it; the dialog exposes `重启代理` and `API 管理` actions. Saves from the existing API manager now write providers/model mappings into `/root/.openclaw/api2py/data/config.json`, managed CLI base URLs use `http://127.0.0.1:9999/v1`, and the old generated Codex 8787 proxy is no longer started. A migration cleanup still kills stale old `codex-proxy.py/js` and any process holding port `8787`.
- 2026-07-18 Codex native icon alignment follow-up: `NativeCodexBrowserView` no longer uses top compound drawables for icon-only TextView buttons; small script/action icons, UA, inspector, elements, and links buttons use left drawables with centered gravity and `includeFontPadding = false`. Script assistant workspace tabs keep text with `iconOnly = false`.
- 2026-07-18 push/build: pushed native browser script-library UI/function parity work to existing GitHub branch `codex-terminal-ime-lag-fix` without creating a new branch/worktree. Initial run `29646867533` selected `6.7 / 186` and failed in Kotlin compilation because `NativeCodexBrowserView.kt` called `nativeRoundedStateDrawable(...)` without the required `Context` receiver. Follow-up fixed those calls to `context.nativeRoundedStateDrawable(...)` and changed workflow build-number discovery to include latest completed runs, so the retry did not reuse failed build `186`.
- 2026-07-18 cloud candidate `6.8 / 187`: retry run `29647690716` selected `6.8 / 187`, passed Flutter analyze, Android arm64 build, APK PRoot verification, collection, and GitHub artifact upload. GitHub artifact is available, but the workflow failed at Gitee split-part upload due to the Gitee network being too slow for the 10-minute per-push timeout.
- 2026-07-18 GitHub-artifact-only delivery change: removed the `Upload APK parts to Gitee transfer branch` step from `.github/workflows/flutter-build.yml`, added a Node test guard that the workflow publishes APK through GitHub artifacts only, and downloaded run `29647690716` locally to `dist/github-run-29647690716/`.
- 2026-07-18 prebuilt RootFS fallback guard: pushed run `29651050217` for the GitHub-artifact-only workflow, then cancelled it after it revealed `Prebuilt rootfs fingerprint mismatch` and started rebuilding RootFS. The workflow now fails fast when the prebuilt RootFS asset is not reusable unless repository variable `BUILD_BUNDLED_ROOTFS=true` is explicitly set.
- 2026-07-18 RootFS reuse correction for next cloud build: `scripts/fetch-prebuilt-rootfs-asset.sh` now treats manifest fingerprint mismatch as a warning, downloads the published `basic-resource` archive, and still enforces archive size/SHA-256 verification. This is intentional so ordinary APK builds reuse the existing prebuilt RootFS and do not silently rebuild RootFS just because app-side source files changed.
- 2026-07-18 cloud candidate `7.0 / 189`: pushed local relay work to existing branch `codex-terminal-ime-lag-fix` via GitHub API, producing remote SHA `34cfcaa437c83abe838c3b4b41c64b7e37b7f733` and Actions run `29656506917`. The run selected build `189`, passed with RootFS reuse, uploaded artifact `ciyuanxia-apks`, and local download/ZIP/APK SHA checks passed.
- 2026-07-18 local relay pip hotfix: user reported installed `7.0 / 189` failed when tapping `重启代理` because RootFS Python had no pip: `/usr/bin/python3: No module named pip`. `LocalApiProxyService` now checks `python3 -m pip --version`, installs `python3-pip` with apt when pip is missing, falls back from `pip install --break-system-packages` to ordinary `pip install`, verifies required modules, and extends dependency-install timeout to 300 seconds.
- 2026-07-18 native Codex/browser automation icon and script-library UI correction: terminal pager `浏览器` now uses `lucide_app_window` instead of search/globe. Native browser open-address, UA, inspector, link, click, run, source, route/steps, import, and Codex-generate actions now use semantically matched Lucide assets (`external-link`, `panel-right/globe`, `scan-search`, `link`, `mouse-pointer-click`, `play`, `file-code`, `route`, `upload`, `bot`). Pure icon buttons in the browser script/inspector surfaces now render as centered `ImageView`s inside `FrameLayout`s instead of left compound drawables on `TextView`, fixing off-center icon placement. The script-library popup no longer uses the stock AlertDialog close button; it has a custom workbench header with refresh/close icon buttons, stats pills, text+icon primary actions, segmented workspace tabs, a framed list region, and clearer workflow/user-script cards. Added matching SVG/PNG Lucide assets and updated third-party notices.
- 2026-07-18 cloud retry note: Actions run `29658130816` selected `7.1 / 190`, reused the prebuilt RootFS, passed Flutter analyze, then failed in Kotlin compilation because `uaButton` was converted to `FrameLayout` but `updateBrowserChrome()` still set TextView-only `gravity` and `minHeight` properties. Build `190` is consumed/reserved and must not be reused. Local follow-up removes those invalid property assignments before retrying.
- 2026-07-18 cloud candidate `7.2 / 191`: pushed Kotlin compile fix to existing branch `codex-terminal-ime-lag-fix` via GitHub API, producing remote SHA `42a029c782aa043360a48547a7dfdbbb5c89596e` and Actions run `29658373259`. The run selected build `191`, passed with RootFS reuse, uploaded artifact `ciyuanxia-apks`, and local download/ZIP/APK SHA checks passed.
- 2026-07-18 local relay startup diagnostics fix: user reported installed `7.2 / 191` still failed after pip installed api2py dependencies successfully, with only `Python 服务启动失败，请查看 /root/.openclaw/api2py/server.log` shown. The bundled `api2py` requirements now remove unused FastAPI, cap the Starlette/Uvicorn/HTTPX/aiosqlite major ranges, and the dependency installer retries against official PyPI if the configured pip index fails. `start.sh` clears stale `server.log` before each launch, kills failed startup processes, and prints the latest 80 log lines; `LocalApiProxyService` also reads and appends the server log tail to final startup errors.
- 2026-07-18 local relay frontend auth removal: user reported the `api2py` frontend still had login validation. The bundled management page now removes the `renderAuth` login/setup overlay, logout button injection, `/api/login` and `/api/setup` frontend calls, and the chat test 401/403 hint no longer tells users to log into the management backend. Backend local-request bypass remains the security boundary for the in-app local management page.
- 2026-07-18 native browser script-library sub-dialog polish: user reported more native popups remained in the browser script library. `NativeCodexBrowserView` now uses a reusable dark script-workbench dialog frame for page snapshots, automation step details, traditional script source view, pending-draft discard, automation metadata save/rename, automation delete, traditional script import/edit/run/delete, automation variable input, and automation run logs. These flows no longer use raw `AlertDialog` title/message/buttons; the only remaining `AlertDialog.Builder` usage in this surface is the custom view container for the workbench-style dialogs and the already-custom script-library/more menu.
- 2026-07-18 native script-library UI/function parity: `NativeCodexBrowserView` script assistant dialog now has a denser dark workbench header, clearer workspace counts, pending-draft card, improved empty states, and native handling for `browser_script_stage/save/run/rename/delete/clear_pending` plus `browser_user_script_list/save/delete` aliases. This keeps the native path closer to the old Flutter script assistant without creating a new branch/worktree.
- 2026-07-18 memory sync: cloud run `29640675284` succeeded after the Gitee timeout work, producing `6.6 / 185`; branch name `codex-gitee-transfer-timeout-186` is a topic name, not the APK build number. Project memory previously lagged at `6.5 / 184` and has been corrected.
- 2026-07-18 native Codex pager/browser rounded icon-button fix: `NativeTerminalPagerActivity` top actions are Lucide-style icon-only rounded buttons with selected/pressed states and `KEYBOARD_TAP` haptics. `NativeTerminalSessionView` keeps Codex shortcut keys rounded with press feedback. `NativeCodexBrowserView` browser nav/open/UA/more/inspector/script-library actions are icon-first rounded controls with haptics, while script-library tabs use selected rounded states.
- 2026-07-18 native Codex pager session + script-library/browser density fix: `NativeTerminalPagerActivity` supports multi-session tabs and removes the rounded outer terminal frame/action-row frame. `NativeCodexBrowserView` script library is dual-workspace, the browser WebView is unframed, controls are compressed into a compact top band, and the more menu is a custom dense list.
- 2026-07-18 earlier governance refresh recorded the verified `6.5 / 184` Gitee transfer path.

## Checks Run
- 2026-07-21 deep unused-code cleanup local checks: `npm test` passed 37/37 with new guards for workflow concurrency and deleted Gitee scripts; `npm run lint -- --no-warn-ignored` passed; `bash -n scripts/build-apk.sh scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/fetch-proot-binaries.sh scripts/prebuilt-rootfs-metadata.sh scripts/publish-prebuilt-rootfs-asset.sh` passed; `python3 -m py_compile scripts/build_release.py scripts/versioning.py` passed; `audit_github_actions.py --project .` passed with no warnings after adding concurrency; `scan_app_secrets.py --project .` found no high-risk secrets, only expected secret-like field names and ignore-rule gaps for `*.p12`/`*.mobileprovision`; `git diff --check` passed. Local Flutter/Dart/adb remain unavailable, so Android compile validation is delegated to GitHub Actions.
- 2026-07-21 unused local-file cleanup checks: searched for ignored temp files and large local files; confirmed `claude-code-2.1.148-bundle.tar.gz` was ignored/untracked and zero-referenced; confirmed `NativeUiStyle.kt` helpers are referenced by native Kotlin surfaces; confirmed bundled sample configs are dynamically loaded by `BundledSampleConfigService`; `python3 -m py_compile flutter_app/assets/api2py/app/*.py flutter_app/assets/api2py/server.py flutter_app/assets/api2py/scripts/migrate_from_php.py` passed; `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh scripts/build-apk.sh` passed; `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Final scan found no remaining `__pycache__`, `.pyc`, or non-`node_modules`/non-`dist` files over 50MB. Local Flutter/Dart/Kotlin compilers remain unavailable.
- 2026-07-20 CodeBuddy max_tokens compatibility hotfix checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `python3 -m py_compile flutter_app/assets/api2py/app/config.py flutter_app/assets/api2py/app/main.py flutter_app/assets/api2py/app/protocol.py flutter_app/assets/api2py/app/proxy.py` passed; `git diff --check` passed. The final fix does not globally clamp proxy output tokens; CodeBuddy now uses a stable local model alias mapped by api2py to the real upstream model so CodeBuddy does not apply a mismatched built-in model max-output profile. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-20 cloud build `8.3 / 202` checks: GitHub Actions run `29722554296` passed RootFS restore/verify without rebuilding RootFS, Flutter analyze, Android arm64 build, APK PRoot native-library verification, artifact collection, and GitHub artifact upload. Local artifact download extracted `CiYuanXia-v8.3-202-arm64-v8a.apk`, APK SHA-256 is `f24cff78206f6eb85238eb7f0f608654b0e61476316e89f6a6b00e9c7feb26c8`, and APK inspection confirmed api2py assets plus the bundled `openclaw-rootfs-noble-arm64.tar.gz` are present.
- 2026-07-20 CLI proxy protocol ownership hotfix checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `python3 -m py_compile flutter_app/assets/api2py/app/config.py flutter_app/assets/api2py/app/main.py` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-20 cloud build `8.2 / 201` checks: GitHub Actions run `29716934002` passed RootFS restore/verify without rebuilding RootFS, Flutter analyze, Android arm64 build, APK PRoot native-library verification, artifact collection, and GitHub artifact upload. Local artifact ZIP SHA-256 matched GitHub artifact digest, `unzip -t` passed, APK SHA-256 is `b02f4743aac2c98fc9a8e01cec872516e73e4deb78dfa98a946842d8a8d3de40`, and APK inspection confirmed api2py assets plus the bundled `openclaw-rootfs-noble-arm64.tar.gz` are present.
- 2026-07-20 Settings battery optimization callback retry fix checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-20 CLI model protocol switching fix checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `python3 -m py_compile flutter_app/assets/api2py/app/config.py flutter_app/assets/api2py/app/main.py` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-19 settings battery-optimization return refresh checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-19 local proxy auth-token visibility checks: `python3 -m py_compile flutter_app/assets/api2py/app/config.py` passed; `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-19 local proxy API config follow-up checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-19 local proxy mapping/browser follow-up checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `bash -n scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh` passed; `git diff --check` passed; app memory validation passed with no warnings. Local Flutter/Dart/Kotlin compilers remain unavailable, so Flutter analyze/test and Android compile still require GitHub Actions.
- 2026-07-19 local proxy recursion/prebuilt reuse/frontend performance checks: `npm test` passed 37/37; `npm run lint -- --no-warn-ignored` passed; `bash -n scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh` passed; `git diff --check` passed; focused `rg` confirmed no `fonts.googleapis`/`Noto Serif` remain in the api2py page and only the scheduler wrapper calls `window.lucide.createIcons()`. Local Flutter/Dart/Kotlin compilers remain unavailable, so `flutter analyze`, Flutter tests, Android compile, and device WebView profiling were not run locally.
- 2026-07-19 local proxy page and Codex launcher hotfix checks: local api2py smoke with `PORT=19999 bash start.sh` verified `GET /`, `GET /static/lib/tailwind.js`, and `GET /static/lib/lucide.js` all return HTTP 200, then `stop.sh` stopped the service and runtime files were removed. `npm test` passed 36/36; `npm run lint -- --no-warn-ignored` passed after ignoring bundled third-party api2py static JS from ESLint; `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh scripts/build-prebuilt-rootfs.sh` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so APK compile/device verification still requires a cloud build.
- 2026-07-19 persistent proxy/RootFS rebuild checks: before commit, `npm test` passed 36/36, `npm run lint -- --no-warn-ignored` passed, `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh` passed, and `git diff --check` passed. GitHub Actions run `29674538930` then passed intentional RootFS rebuild/publish/verify, Flutter analyze, Android arm64 APK build, APK PRoot native-library verification, artifact collection, and artifact upload. Local artifact ZIP SHA matched GitHub digest, `unzip -t` passed, and APK SHA-256 was recorded.
- 2026-07-18 api2py missing package hotfix checks: inspected `dist/github-run-29659937270/CiYuanXia-v7.3-192-arm64-v8a.apk` with `unzip -l` and confirmed it omitted nested `assets/api2py/app/` files, matching the device error `ModuleNotFoundError: No module named 'app'`. `npm test` passed 36/36; `npm run lint -- --no-warn-ignored` passed; `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh scripts/build-prebuilt-rootfs.sh` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so a new APK build is required to verify the corrected Flutter asset packaging. A separate intentional RootFS rebuild/publish is required before api2py dependencies are truly preinstalled in first-run RootFS.
- 2026-07-18 api2py direct-entry/9999 relay replacement checks: `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Source search confirmed old `8787` proxy startup is removed from CLI runtime templates; the only intentional source `8787` left is stale-process migration cleanup in `CliApiConfigService` plus test guards/history notes. Local Flutter/Dart/Kotlin compilers remain unavailable.
- 2026-07-18 RootFS reuse workflow correction checks: `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed; `bash -n scripts/fetch-prebuilt-rootfs-asset.sh` passed. A local trial confirmed the script warns on the current `basic-resource` fingerprint mismatch and proceeds to download/verify the published archive; the local download was stopped before completion to avoid blocking the push and the partial archive was restored to the committed LFS pointer.
- 2026-07-18 cloud build `7.0 / 189` checks: GitHub Actions run `29656506917` passed RootFS restore/verify, Android SDK setup, Flutter analyze, arm64 APK build, APK PRoot native-library verification, artifact collection, and GitHub artifact upload. Local artifact download via API/aria2c passed; ZIP SHA-256 matched GitHub artifact digest, `unzip -t` passed, and extracted APK SHA-256 is `12b54801441202d9395a96b63b60abe2452969d82ca4bba2a5cdb6dfcf78a397`.
- 2026-07-18 local relay pip hotfix checks: `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so cloud build is required for compile verification.
- 2026-07-18 native icon/script-library UI correction checks: `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed; a source/resource scan verified all `R.drawable.lucide_*` references in `NativeCodexBrowserView.kt` and `NativeTerminalPagerActivity.kt` have matching PNG assets. Local Flutter/Dart/Kotlin compilers remain unavailable, so Android compile/device verification still requires cloud build or Android SDK environment after user approval.
- 2026-07-18 failed cloud compile `7.1 / 190`: GitHub Actions run `29658130816` passed RootFS restore/verify, skipped RootFS build/publish, passed Flutter analyze, then failed in `:app:compileReleaseKotlin` at `NativeCodexBrowserView.kt` unresolved `uaButton.gravity` and `uaButton.minHeight` after `uaButton` became a `FrameLayout`.
- 2026-07-18 cloud build `7.2 / 191` checks: GitHub Actions run `29658373259` passed RootFS restore/verify without rebuilding RootFS, Flutter analyze, Android arm64 build, APK PRoot native-library verification, artifact collection, and GitHub artifact upload. Local artifact download via GitHub API passed; ZIP SHA-256 matched GitHub artifact digest, `unzip -t` passed, and extracted APK SHA-256 is `10bfcd7ca6c5de5de2e6562b7455cb45a474bbd2a8d9114e02c26bad75a68286`.
- 2026-07-18 local relay startup diagnostics checks: local Python venv with official PyPI installed the capped api2py requirements and `PORT=19999 bash start.sh` returned `/api/health` OK; generated runtime artifacts were removed from bundled assets afterward. `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh` passed; `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so APK compile/device verification still requires cloud build after user approval.
- 2026-07-18 local relay frontend auth removal checks: source search confirmed `flutter_app/assets/api2py/public/static/index.html` no longer contains frontend login/setup/logout UI or `/api/login`/`/api/setup` calls. `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so APK compile/device verification still requires cloud build after user approval.
- 2026-07-18 native browser script-library sub-dialog polish checks: source search confirmed `NativeCodexBrowserView.kt` no longer contains script-library `setTitle`, `setMessage`, `setPositiveButton`, or `setNegativeButton` calls for sub-dialogs, and all `R.drawable.lucide_*` references in that file have matching PNG assets. `npm test` 35/35 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Android compile/device verification still requires cloud build after user approval.
- 2026-07-18 push/build retry checks: `npm test` 32/32 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed; `bash -n scripts/upload-apk-parts-to-gitee-branch.sh scripts/build-apk.sh scripts/build-prebuilt-rootfs.sh scripts/prebuilt-rootfs-metadata.sh scripts/fetch-prebuilt-rootfs-asset.sh` passed. GitHub Actions run `29647690716` passed Flutter analyze, Kotlin/Gradle APK build, and APK PRoot verification, then failed only in the Gitee split upload step.
- 2026-07-18 GitHub-artifact-only delivery checks: `npm test` 33/33 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check -- .github/workflows/flutter-build.yml lib/test.js` passed; `sha256sum dist/github-run-29647690716/ciyuanxia-apks.zip` matched GitHub artifact digest; `unzip -t dist/github-run-29647690716/ciyuanxia-apks.zip` passed; extracted APK SHA-256 is `df8144fa887bc8648684a5d0105e5b2be0ded157adfd0e8551b7b5213e0105c3`.
- 2026-07-18 native script-library parity checks: `npm test` 32/32 passed; `npm run lint -- --no-warn-ignored` passed; `git diff --check -- NativeCodexBrowserView.kt lib/test.js` passed. Local Flutter/Dart/Kotlin compilers remain unavailable, so Android compile verification still requires GitHub Actions or a full SDK environment.
- 2026-07-18 cloud-build memory sync: `gh run list` and `gh run view 29640675284 --log` confirmed successful cloud build `6.6 / 185`, artifact ID `8428690154`, artifact digest `sha256:c4344f874ef2a64958c570eda23690b693444361e0d8c55aedc07d8f0889517c`; artifact download was started then cancelled at user request because the user had already downloaded/installed it.
- 2026-07-18 project-management governance pass: `validate_app_memory.py --project .` passed with no errors or warnings; `inspect_app_project.py --project .` ran but only auto-detected the root Node shell, so Flutter/Kotlin facts were manually verified from `flutter_app/pubspec.yaml`, `flutter_app/test/`, and `.github/workflows/flutter-build.yml`. No business-code checks were rerun in this governance-only pass.
- 2026-07-18 rounded icon-button UI checks: `npm test` 32/32; `npm run lint -- --no-warn-ignored` passed; `git diff --check` passed. Source guards cover pager new-session icon controls, rounded state drawables, haptic feedback, dual-workspace script assistant, browser icon actions, inspector icons, script-library icon mappings, unframed browser, compact controls, and custom more menu.
- Local Flutter/Dart/Kotlin compilers remain unavailable.
- Cloud build launched after user request; latest run `29647690716` produced GitHub artifact `6.8 / 187` but failed final Gitee split transfer.

## Cloud Build Status
- Latest requested cloud build: GitHub Actions run `29867482912` completed successfully on branch `codex-terminal-ime-lag-fix` at remote SHA `c8a87a3384edf27350e7f4125f679363ec0d87b0`. It selected logical build `204`, semantic `8.5.0`, display `8.5`, APK `CiYuanXia-v8.5-204-arm64-v8a.apk`, artifact ID `8509966202`, digest `sha256:0efb1d1438808a284c6591a2a1835cf809426006cbd72db13671c9f7764074b3`, and uploaded only to GitHub artifact storage. RootFS was reused; RootFS build/publish steps were skipped. Local artifact download/ZIP/APK verification passed under `dist/github-run-29867482912/`.
- Latest requested cloud build: GitHub Actions run `29716934002` completed successfully on branch `codex-terminal-ime-lag-fix` at remote SHA `d0f85aa86de49ecda1bf8060976f7502dbf165da`. It selected logical build `201`, semantic `8.2.0`, display `8.2`, APK `CiYuanXia-v8.2-201-arm64-v8a.apk`, artifact ID `8451064233`, digest `sha256:9d013f65db1a4299709fd6426c68ecef3c929bd378a401d2798fc23d73ab07d7`, and uploaded only to GitHub artifact storage. RootFS was reused; RootFS build/publish steps were skipped. Local artifact download/ZIP/APK verification passed under `dist/github-run-29716934002/`.
- Latest requested cloud build: GitHub Actions run `29658373259` completed successfully on branch `codex-terminal-ime-lag-fix` at remote SHA `42a029c782aa043360a48547a7dfdbbb5c89596e`. It selected logical build `191`, semantic `7.2.0`, display `7.2`, APK `CiYuanXia-v7.2-191-arm64-v8a.apk`, artifact ID `8433612396`, digest `sha256:dfcc0cd6fe50573134f95583f2022d91ca7a962fa2d681102d79f1b0e67d3213`, and uploaded only to GitHub artifact storage. No Gitee upload step ran.
- Cancelled workflow-only run: GitHub Actions run `29651050217` on remote SHA `5403e945357cbda8351eceb6b648c7fde012e89d` was cancelled after `Restore bundled OpenClaw rootfs` reported `Prebuilt rootfs fingerprint mismatch` and the old workflow started rebuilding RootFS. It produced no APK artifact. Use it as evidence that default RootFS fallback must remain disabled unless intentionally rebuilding resources.
- RootFS guard verification run: GitHub Actions run `29651658512` on remote SHA `4875d9a008a94eb1db531ac4942a632e534ae5d7` failed fast in 35 seconds at `Restore bundled OpenClaw rootfs` with `Prebuilt rootfs fingerprint mismatch` and the explicit error telling maintainers to set `BUILD_BUNDLED_ROOTFS=true` only for intentional RootFS rebuild/publish. `Build bundled OpenClaw rootfs`, APK build, and artifact upload were skipped; no APK artifact was produced.
- Latest published release: `5.4 / 173`.
- Latest GitHub artifact candidate: `8.5 / 204`.
- Latest fully Gitee-delivered candidate: `6.6 / 185`.

## Memory Validation
- 2026-07-21 governance pass: `validate_app_memory.py --project .` passed with no errors and one warning because the previous latest session `20260720-130000-cloud-build-8-3-202.md` used `## Artifact` instead of the required `## Version And Artifacts` heading. This session adds a new compliant handoff.
- 2026-07-21 cloud-state sync: `gh run view 29740833706 --json databaseId,displayTitle,headBranch,headSha,conclusion,status,createdAt,updatedAt,url,event,workflowName` confirmed run `29740833706` is completed successfully at head SHA `ff7c6f6299ac2845a9af01e21b49fd0599fde196`; memory validation was clean before this sync.
- `inspect_app_project.py --project .` still auto-detects only the root Node shell, so Flutter/Kotlin facts must continue to be verified manually from `flutter_app/` and `.github/workflows/flutter-build.yml`.
- `inspect_local_environment.py --project .` confirmed local Node/npm/Java/Gradle are available, but Flutter, Dart, and adb are missing; local APK compile, Flutter tests, and device smoke remain unavailable in this Termux environment.

## Risks And Blockers
- CodeBuddy max_tokens compatibility fix is local source only until the next APK cloud build. Device smoke should verify the same model that works in Codex no longer fails in CodeBuddy with `max_tokens exceeds the limit of 65536`, and that normal Codex tool-calling still works through the local proxy. Do not reintroduce a global `65536` output-token clamp in api2py; high-output models may support larger limits.
- Current proxy persistence, rebuilt RootFS dependency preinstall, Codex default-config fixes, mapping-preservation fixes, proxy browser layout changes, proxy API management UI refinements, CLI proxy protocol ownership hotfix, Settings battery-optimization callback retry, CodeBuddy local model alias compatibility, and obsolete Gitee script cleanup are packaged in local APK candidate `8.5 / 204`. Device smoke must verify app launch auto-starts the proxy, `重启代理` leaves `127.0.0.1:9999/api/health` responsive, `API 管理` loads with Tailwind/Lucide, proxy status changes in the dialog, Codex no longer executes `/root/.config` or enters ChatGPT account auth when using the local 9999 proxy, proxy/provider/model mappings are preserved without recursive local-proxy entries, api2py backend protocol edits survive opening Codex and other CLI terminals, old stale Codex `apiProtocol` values no longer override backend mappings, explicit CLI dropdown protocol changes update the selected mapping once, the api2py mapping dropdowns fit inside the bounded route-table card, returning from Android battery optimization settings refreshes without a second tap, and CodeBuddy no longer fails with `max_tokens exceeds the limit of 65536` through the same local relay/model that works in Codex.
- Installed `7.3 / 192` cannot start api2py because the APK omitted nested api2py asset directories; fix is local only until the next APK build. Preinstalling api2py dependencies into RootFS requires an explicit `basic-resource` rebuild/publish and should not be mixed into ordinary APK builds unless the user approves the resource rebuild.
- Gitee split-part upload was removed from the main APK workflow after timeout on run `29647690716`; do not restore it without a new explicit distribution strategy.
- Prebuilt RootFS release asset currently mismatches the source-tree fingerprint used by runs `29651050217` and `29651658512`; the next workflow now reuses the published archive after size/SHA verification instead of rebuilding RootFS. If the release asset is missing or fails SHA/size/gzip checks, the workflow still fails instead of silently rebuilding unless `BUILD_BUNDLED_ROOTFS=true` is intentionally set.
- Native script assistant now handles native `browser_script_stage` pending drafts, but existing Flutter-side in-memory drafts from the old `TerminalBrowserPanel` are still separate if that fallback panel is used in the same session.
- Device smoke still required for multi-session and script-library interactions.
- Branch topology remains divergent; promotion must name exact remote SHA.
- Local Termux cannot compile Flutter/Kotlin.

## Next Actions
1. Device-smoke local `8.5 / 204` from `dist/github-run-29867482912/CiYuanXia-v8.5-204-arm64-v8a.apk`: verify CodeBuddy through `http://127.0.0.1:9999/v1` with the same model that works in Codex and confirm the previous `500 max_tokens exceeds the limit of 65536` error is gone.
1. Device-smoke local `8.5 / 204` as the active candidate for all `8.4 / 203` and `8.3 / 202` proxy/API-management regressions too: verify first setup with prebuilt RootFS reuses OpenClaw without npm/download reinstall; original `管理 API` shows/selects the built-in `本地中转代理` profile with default key `sk-123`; saving API profiles never writes `127.0.0.1:9999/v1` into api2py providers; adding upstream providers appends/preserves proxy-side providers and mappings; protocol options match `openai/responses/anthropic/ollama`; api2py backend protocol edits must survive opening Codex and other CLI terminals; stale saved Codex `apiProtocol` values must not override proxy mappings; explicit CLI dropdown changes should update only the selected mapping once; non-Codex CLI protocol switching should take effect on the intended alias; model choices show upstream name/address/model; the api2py route-table card height is bounded so dropdowns are usable with few rows; the proxy browser keeps address/status at the top with evenly centered back/forward/refresh controls at the bottom while avoiding Codex automation-browser IME compensation lag; selecting unrestricted/ignore battery optimization in Android settings should update the Settings row automatically after returning.
2. Device-smoke local `7.6 / 195` from `dist/github-run-29674538930/CiYuanXia-v7.6-195-arm64-v8a.apk`: verify first launch auto-starts api2py through the persistent native foreground service, `中转代理` status reports responsive, `重启代理` keeps `127.0.0.1:9999/api/health` reachable, `API 管理` loads the api2py management page, Codex no longer fails with `/root/.config: Permission denied`, and Codex uses API-key auth through `http://127.0.0.1:9999/v1` instead of ChatGPT login bootstrap.
2. Future ordinary APK builds should reuse the updated `basic-resource` RootFS; do not rebuild RootFS again unless the user explicitly requests another resource update.
4. Device-smoke the local Codex pager multi-session controls after the next installable build the user requests.
2. Device-smoke the native Codex/browser icon and script-library UI correction after the next installable build: terminal/browser tab icons, browser open-address/UA/more controls, inspector toggle/elements/links, script-library action icons, icon centering inside every small rounded button, custom script-library header/close button, stats pills, segmented tabs, card readability, and pressed/selected states.
3. Device-smoke the dual-workspace native script assistant: stage pending draft, save/edit pending draft, save recent flow, automation run/rename/copy/delete, traditional script add/import/edit/run/delete/copy.
4. If old Flutter fallback panel and native pager must share drafts live, design an explicit cross-controller draft bridge; do not assume their in-memory pending drafts are shared.
5. Device-smoke local `7.3 / 192` from `dist/github-run-29659937270/CiYuanXia-v7.3-192-arm64-v8a.apk`; verify `重启代理` can recover on RootFS images where `python3 -m pip` is initially unavailable, pip index fallback works, `/api/health` starts on `9999`, frontend login/setup is gone, and any future Python startup failure includes `server.log` tail in the app error.
6. Only on explicit request, package another cloud build with logical build `> 204`; do not reuse `186`, `187`, `188`, `189`, failed build `190`, artifact candidates `191` through `204`, or any other consumed historical build number.
