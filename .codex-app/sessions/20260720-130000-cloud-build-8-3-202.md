# Session 2026-07-20 13:00 UTC - Cloud Build 8.3 / 202

## Goal
- User requested `提交构建` after the CLI proxy protocol ownership hotfix.

## Repo Facts Read
- Local working directory: `/storage/emulated/0/ZeroTermux/开发/openclaw-termux-zh-5.5`.
- Existing branch only: `codex-terminal-ime-lag-fix`; no new branch/worktree was created.
- Local commit built: `d8e3e01 fix: preserve proxy protocol mappings`.
- GitHub API push updated remote branch from `d0f85aa` to `1e07027553ccba61ce5fc8aee592b99470e73bfb`.

## Changes Made
- `CliApiConfigService.saveToolSettings()` clears stale tool-level `apiProtocol` when the selected profile is the built-in local proxy.
- `_resolvedToolConfig()` no longer lets stale tool-level `apiProtocol` override the local proxy profile protocol.
- `_toolSettingsJson()` persists empty `apiProtocol` for direct local-proxy base URLs.
- `_mergeLocalApiProxyConfig()` preserves existing proxy-side mapping `protocol` first; tool-level `apiProtocol` only initializes a brand-new mapping.
- `CliApiConfigDialog` loads proxy-side mapping protocol for any alias and applies explicit dropdown protocol changes through `updateLocalApiProxyModelProtocol()`.
- `lib/test.js` guards the proxy protocol ownership behavior.
- `.codex-app/` was updated with build and handoff records.

## Checks Run
- `npm test` passed 37/37.
- `npm run lint -- --no-warn-ignored` passed.
- `python3 -m py_compile flutter_app/assets/api2py/app/config.py flutter_app/assets/api2py/app/main.py` passed.
- `git diff --check` passed.
- App memory validation passed with no errors or warnings before push.

## Cloud Build
- GitHub Actions run: `29722554296`.
- Workflow: `Build OpenClaw Apps`.
- Branch: `codex-terminal-ime-lag-fix`.
- Head SHA: `1e07027553ccba61ce5fc8aee592b99470e73bfb`.
- Conclusion: success.
- Version/build: display `8.3`, semantic `8.3.0`, Android build `202`.
- APK name: `CiYuanXia-v8.3-202-arm64-v8a.apk`.
- RootFS policy: reused published `basic-resource`; `Restore bundled OpenClaw rootfs` and `Verify bundled OpenClaw rootfs` passed; `Build bundled OpenClaw rootfs` and `Publish bundled OpenClaw rootfs` were skipped.
- Build steps passed: Flutter analyze, Android arm64 APK build, APK PRoot native-library verification, artifact collection, GitHub artifact upload.
- No Gitee upload step ran.

## Artifact
- GitHub artifact: `ciyuanxia-apks`, ID `8453011306`, size `311436541` bytes, digest `sha256:8c720b776fcf36cb4c705c66d12c5e153895bd367ce62c3adc89b45eba90204f`, expires `2026-08-19T06:51:01Z`.
- Local APK: `dist/github-run-29722554296/CiYuanXia-v8.3-202-arm64-v8a.apk`, size `325175498` bytes, SHA-256 `f24cff78206f6eb85238eb7f0f608654b0e61476316e89f6a6b00e9c7feb26c8`.
- APK inspection confirmed these key assets are present: `assets/flutter_assets/assets/api2py/app/config.py`, `app/main.py`, `data/config.example.json`, `public/static/index.html`, `public/static/lib/lucide.js`, `public/static/lib/tailwind.js`, and `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz`.

## Known Risks
- Android device smoke is still required. In particular, changing a mapping protocol in the api2py backend must survive opening Codex and other CLI terminals; stale saved Codex `apiProtocol` values must not override proxy mappings; explicit CLI dropdown changes should update only the selected mapping once.
- Local Flutter tests are still not run locally because the Termux environment has no Flutter SDK; GitHub Actions ran Flutter analyze and APK build only.

## Next Actions
- Device-smoke local `8.3 / 202` from `dist/github-run-29722554296/CiYuanXia-v8.3-202-arm64-v8a.apk`.
- Next fresh cloud build must use logical build greater than `202`.
- Keep ordinary APK builds on prebuilt RootFS reuse unless the user explicitly asks for another RootFS resource rebuild.
