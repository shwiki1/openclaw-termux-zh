# 2026-07-16 05:58 UTC - v4.3.0 Arm64 Release

## Goal

Publish the browser sidecar persistence, script workspace, browser contrast, and Codex terminal IME-close performance changes, then download and verify the release APK locally.

## Repo Facts Read

- The GitHub workflow `.github/workflows/flutter-build.yml` produces only `arm64-v8a` APK artifacts and creates a GitHub Release.
- Local Flutter and Dart SDK commands are unavailable in this Termux environment; Flutter analysis and Android packaging therefore run in GitHub Actions.

## Changes Made

- No source changes in this handoff. Recorded the completed cloud release and its verified local artifact.

## Checks Run

- Pre-submit checks from the source handoff: `npm test` (30 passed), `npm run lint`, and `git diff --check` passed.
- GitHub Actions run `29474846658` completed successfully: `Build arm64-v8a APK` and `Create GitHub Release` both passed.
- Local artifact checks passed: `unzip -t`, `zipalign -c -v 4`, and `apksigner verify --verbose --print-certs`.

## Cloud Build

- Remote commit: `c323ac3b641a41c2530d20ab64557913aa21d470` on GitHub `main`.
- Release: `v4.3.0`, install-visible version `4.3`, logical build `162`, arm64-v8a only.
- Earlier failed attempts `29473924386` (build `160`) and `29474370249` (build `161`) failed before artifact creation; both build numbers remain reserved.

## Version And Artifacts

- Local APK: `dist/github-release-v4.3.0/CiYuanXia-v4.3-162-arm64-v8a.apk`.
- Size: `316450107` bytes.
- SHA-256: `6b08709b66b1c57d0fe480673ad6183f46b087997b3462ec2b135108e2529ca0`.
- Release signer SHA-256: `0618eafd1855855749abb7c04d6f44edf9a4b7cb09e26fd882e856d5c994dde6`.

## Known Risks

- Device validation is still required because this environment cannot run Flutter tooling or Android UI smoke tests.

## Next Actions

1. On Android, verify a browser tab survives closing and reopening the compact sidecar while Codex browser commands run.
2. Verify swipe and selector navigation between `Codex 自动化` and `传统脚本`, and browser button foreground contrast on dark surfaces.
3. With a long Codex CLI transcript visible, close the IME and verify terminal input resumes smoothly without the previous multi-second stall.
