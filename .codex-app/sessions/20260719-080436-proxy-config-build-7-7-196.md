# 2026-07-19 08:04 UTC - Proxy Config Merge And Build 7.7

## Goal
Fix local relay/original API manager config conflicts, reduce local relay browser lag, avoid unnecessary OpenClaw reinstall from narrow prebuild detection, and deliver a fresh APK without rebuilding RootFS.

## Repo Facts Read
- Flutter/Kotlin app in `flutter_app/` with PRoot RootFS and api2py relay under `flutter_app/assets/api2py/`.
- Existing branch: `codex-terminal-ime-lag-fix`; no new branch/worktree created.
- GitHub Actions APK workflow uses artifact `ciyuanxia-apks` and can reuse `basic-resource` RootFS.

## Changes Made
- `CliApiConfigService` now reads existing `/root/.openclaw/api2py/data/config.json` and merges App-managed providers/model mappings instead of replacing the whole proxy config. Existing proxy UI providers, tokens, routes, pricing, admin account, and concurrency config are preserved.
- Added `openclaw_app_managed_model_aliases` so later saves only remove/update the App-managed model aliases from the previous save.
- `LocalApiProxyBrowserScreen` keeps its WebView state alive, uses an opaque background, and avoids redundant `setState` churn for status/navigation updates.
- `BootstrapManager` now detects OpenClaw through wrapper binaries and alternate npm/package layouts, not only `/usr/local/lib/node_modules/openclaw/package.json`.
- `OpenClawVersionService` now reads installed version from multiple package-json layouts.
- `.github/workflows/flutter-build.yml` now includes completed manual `workflow_dispatch` runs when choosing the next Android build number, preventing duplicate build numbers after manual builds.
- `lib/test.js` covers proxy config merge behavior, OpenClaw alternate layout detection, RootFS reuse, and the build-number workflow guard.

## Checks Run
- `npm test` passed: 37/37.
- `npm run lint -- --no-warn-ignored` passed.
- `git diff --check` passed.
- `bash -n scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh` passed.
- `dart format` was skipped locally because `dart` is not installed in the Termux environment; GitHub `flutter analyze` passed.
- Memory validation passed with no warnings after this session update.

## Cloud Build
- Remote functional commit via GitHub API: `8cd3ef78051b96b87e75d5a552721775fdbaca49`.
- Remote workflow-fix commit via GitHub API: `660cdc94373a3b2c27e798763043699ac5f174c0`.
- Superseded run `29677549463` succeeded but produced duplicate `7.6 / 195`; do not deliver it.
- Valid run `29678484884` succeeded with `rebuild_rootfs=false`; RootFS restore passed, RootFS build/publish skipped, Flutter analyze and APK build passed.

## Version And Artifacts
- Valid artifact: `7.7 / 196`.
- GitHub artifact: `ciyuanxia-apks`, ID `8439821802`, digest `sha256:4ac2a75d0b389d6a3c827193ff83c7351f69ff7550bceb4f1d2c04437a40a9e4`.
- Local ZIP: `dist/github-run-29678484884/ciyuanxia-apks.zip`, SHA-256 `4ac2a75d0b389d6a3c827193ff83c7351f69ff7550bceb4f1d2c04437a40a9e4`; `unzip -t` passed.
- Local APK: `dist/github-run-29678484884/CiYuanXia-v7.7-196-arm64-v8a.apk`, SHA-256 `c7a306d12ea990a53e92c071e0f748f4a40a429af145c6123c92536b5eb5de38`.
- APK inspection confirmed api2py core files and bundled RootFS are present.

## Known Risks
- No Android device smoke test was run locally.
- Existing `.codex-app` files had uncommitted memory changes before this work; functional commits intentionally excluded those files.

## Next Actions
- Install `CiYuanXia-v7.7-196-arm64-v8a.apk` and smoke test: proxy status, proxy management page, original API manager save after adding proxy-side API, and OpenClaw setup not reinstalling when prebuilt files exist.
