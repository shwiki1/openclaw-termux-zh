# 2026-07-16 04:40 UTC - Release v4.0.0

## Goal

Publish the browser automation hardening and dual script assistant, then retrieve the signed arm64 APK locally.

## Repo Facts Read

- `AGENTS.md` permits only Android `arm64-v8a` release artifacts.
- `.github/workflows/flutter-build.yml` derives release display versions from source anchor `2.5.0+143` and minimum logical build `159`.
- Local Flutter/Dart tooling is unavailable; GitHub Actions is the native build authority.

## Changes Made

- Completed the GitHub API push and cloud release after correcting two failed pre-artifact builds.
- Recorded the successful artifact provenance and local verification in `.codex-app/state.md` and `.codex-app/build.md`.

## Checks Run

- Earlier source checks passed: `npm run lint`, `npm test` (30 passed), and `git diff --check`.
- Local release checks passed: `sha256sum`, `unzip -t`, `zipalign -c -v 4`, and `apksigner verify --verbose --print-certs`.
- Flutter/Dart local checks remain unavailable in Termux.

## Cloud Build

- GitHub Actions run `29470132394` passed at remote commit `b8db7a3`.
- Flutter analysis, arm64 APK build, PRoot verification, artifact upload, and GitHub Release creation passed.
- Failed runs `29469493745` (build `157`) and `29469858076` (build `158`) produced no artifacts; both build numbers remain reserved.

## Version And Artifacts

- Release: `v4.0.0`; installer/app display: `4.0`; logical build: `159`.
- Artifact: `dist/github-release-v4.0.0/CiYuanXia-v4.0-159-arm64-v8a.apk`.
- SHA-256: `33553448397646aa7e0f5405c19aebe449e2f883baa247456c5586630385fd24`.
- Signer SHA-256: `0618eafd1855855749abb7c04d6f44edf9a4b7cb09e26fd882e856d5c994dde6`.

## Known Risks

- Android device smoke testing is still required for the enhanced WebView automation and dual script assistant.
- Local branch history differs from API-created GitHub `main`; use the GitHub API helper with the correct base tree for later remote submissions.

## Next Actions

1. Install and device-smoke the `4.0 / 159` arm64 APK.
2. Verify health recovery, mobile UA, paste, resource waiting, and script import/run flows on representative AI creation sites.
3. Reconcile local branch history with remote `main` before unrelated future submissions.
