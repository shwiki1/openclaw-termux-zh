# Session 2026-07-19 06:21 UTC - Proxy Persistence RootFS Build 7.6/195

## User Request
- Installed `7.5 / 194` still reported proxy restart success but page/status failed with `Connection refused` on `127.0.0.1:9999/api/health`.
- Codex CLI still failed with `/root/.openclaw/bin/codex: 23: /root/.config: Permission denied` and ChatGPT account bootstrap `plan type is required`.
- Fix proxy availability, prebuild required api2py dependencies into RootFS, update the cloud reusable prebuilt package, configure Codex defaults using current Termux Codex config and `/storage/emulated/0/ZeroTermux/开发/codex-termux使用教程/`.

## Root Cause
- api2py was being started from a one-shot PRoot command built with `--kill-on-exit`. The app could report start success, then PRoot exited and killed the background Python server, leaving port `9999` closed.
- Codex generated launcher/config still had a multiline `mkdir -p` shape that could execute `/root/.config` as a command, and generated auth could fall into ChatGPT account bootstrap instead of API-key auth.

## Code Result
- Local commit: `43c5a9b fix: keep local proxy alive in proot`.
- GitHub API-created remote SHA: `5bfb21243c9e9397b26bcf790a68a11aba9b3d6f` on existing branch `codex-terminal-ime-lag-fix`.
- Added native `LocalApiProxyForegroundService` so api2py runs as a persistent Android foreground service holding a foreground PRoot process with `exec python3 server.py`.
- Added Flutter/native bridge methods to start/stop/check the local proxy service and changed restart/status to use the persistent service.
- Kept dependency/file checks in Flutter before launching the persistent service.
- Codex defaults now match the current working Termux Codex style: `auth.json` with `OPENAI_API_KEY`, `preferred_auth_method = "apikey"`, `sandbox_mode = "danger-full-access"`, `approval_policy = "never"`, and local proxy base URL `http://127.0.0.1:9999/v1`.

## Checks Before Cloud Build
- `npm test`: 36/36 passed.
- `npm run lint -- --no-warn-ignored`: passed.
- `bash -n flutter_app/assets/api2py/start.sh flutter_app/assets/api2py/stop.sh scripts/build-prebuilt-rootfs.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh`: passed.
- `git diff --check`: passed.
- Local Flutter/Dart/Kotlin compilers remain unavailable.

## Cloud Build
- Workflow: `Build OpenClaw Apps` run `29674538930`.
- Trigger: manual `workflow_dispatch` with `rebuild_rootfs=true`; no Gitee upload step.
- Branch/SHA: `codex-terminal-ime-lag-fix` / `5bfb21243c9e9397b26bcf790a68a11aba9b3d6f`.
- Version: display `7.6`, semantic `7.6.0`, Android build `195`.
- Result: success.
- Passed steps included RootFS build, RootFS publish, RootFS verify, Flutter analyze, arm64 APK build, APK PRoot native-library verification, artifact collection, and artifact upload.

## RootFS Published
- Release: `basic-resource`.
- RootFS asset: `openclaw-rootfs-noble-arm64.tar.gz`.
- Size: `279574832` bytes.
- Digest: `sha256:a5793035aec8491f68782ddefeeabd66c643ba5ddb0e4295b2de9a65dfe37e3d`.
- Manifest digest: `sha256:1fddb20e3e8794d4e3bd2775e96a7f277ab57cd4652e57ec5b482a1800940d62`.
- Build log confirmed base packages include `python3-pip`, `git`, `make`, `g++`, `curl`, `wget`, `lsof`.
- Build log confirmed api2py dependency preinstall: `starlette`, `uvicorn`, `httpx`, `aiosqlite`, plus transitive packages.

## Artifact
- GitHub artifact: `ciyuanxia-apks`, ID `8438660736`.
- Artifact digest: `sha256:ab4381a0f2529f6e771f0953ce2aab3cb1b140dc5e35ce8afcccdde785c07a8a`.
- Artifact size: `311422695` bytes.
- Local ZIP: `dist/github-run-29674538930/ciyuanxia-apks.zip`.
- ZIP SHA-256 matched GitHub digest; `unzip -t` passed.
- Extracted APK: `dist/github-run-29674538930/CiYuanXia-v7.6-195-arm64-v8a.apk`.
- APK SHA-256: `87a62f9098b6732051ed08ed0a309e130abcc7954dde43e671b1a48c24001660`.
- APK inspection confirmed bundled `api2py/server.py`, `requirements.txt`, `app/config.py`, `app/main.py`, `public/static/index.html`, `public/static/lib/lucide.js`, `public/static/lib/tailwind.js`, and `assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz`.

## Next Device Smoke
- Install `dist/github-run-29674538930/CiYuanXia-v7.6-195-arm64-v8a.apk`.
- Verify first launch auto-starts api2py, `中转代理` shows responsive status, and `重启代理` leaves `127.0.0.1:9999/api/health` reachable.
- Open `API 管理` and confirm the api2py management page loads with Tailwind/Lucide.
- Launch Codex and verify `/root/.config: Permission denied` is gone and Codex uses API-key auth via `http://127.0.0.1:9999/v1` instead of ChatGPT login bootstrap.

## Notes
- Future ordinary APK builds should reuse the newly published `basic-resource` RootFS. Do not rebuild RootFS again unless explicitly requested.
- Next fresh cloud build must use logical build greater than `195`.
