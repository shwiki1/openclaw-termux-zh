# 2026-07-22 Cloud Build 9.4

## Goal
- Submit the current settings/proxy restoration work to GitHub Actions and deliver a locally downloaded APK artifact.

## Repo Facts Read
- Repository is a Flutter Android app with Kotlin native services and a PRoot RootFS asset restored during GitHub Actions builds.
- The cloud workflow is still named `Build OpenClaw Apps`, but build steps now restore and verify the CiYuanXia RootFS path.
- Local Flutter/Dart/adb are unavailable; Android APK packaging must be validated in GitHub Actions.

## Changes Made
- Fixed the restored `FloatingFileManagerService.kt` Kotlin compile failure by adding a minimal local media metadata toolbox based on Android `MediaMetadataRetriever`.
- Avoided adding FFmpeg dependencies because the current Gradle dependency set does not include FFmpegKit and adding it would increase APK size for an auxiliary file-manager action.
- Media info/album art reading now compiles; media conversion/extraction actions return `当前版本未内置媒体转码组件` instead of referencing a missing toolbox.

## Checks Run
- `npm test` passed 41/41.
- `npm run lint -- --no-warn-ignored` passed.
- `git diff --check` passed.
- GitHub Actions run `29964868198` passed RootFS restore/verify, Flutter analyze, arm64 APK build, APK PRoot native-library verification, artifact collection, and artifact upload.
- ZIP `unzip -t` passed for `dist/github-run-29964868198/ciyuanxia-apks.zip`.
- APK `unzip -t` passed for `dist/github-run-29964868198/CiYuanXia-v9.4-213-arm64-v8a.apk`.
- APK inspection confirmed bundled RootFS, open-source notice assets, and arm64 PRoot libraries are present.

## Cloud Build
- Failed run `29964401664` exposed the missing `MediaToolbox` Kotlin references in the restored floating file manager.
- Successful run `29964868198` used remote commit `134acc23d7548d31296cdf27955488f4876f73ad` and completed all APK build/upload steps.

## Version And Artifacts
- Failed predecessor: run `29964401664`, remote commit `a975b2711c2866fc5ebf24b2efa4a3149302a2a2`, selected `9.3 / 212`, failed in Kotlin compile; build `212` is consumed/reserved.
- Successful run: `29964868198`.
- Remote commit: `134acc23d7548d31296cdf27955488f4876f73ad`.
- Version: `9.4.0`, display `9.4`, Android build `213`.
- Artifact: `ciyuanxia-apks`, ID `8547547827`, size `216151947` bytes, digest `sha256:a8dba5d8a26c1d239932a34bce06d0e0e085bed27cdfc8355f2588eb5f3544e3`.
- Local ZIP: `dist/github-run-29964868198/ciyuanxia-apks.zip`, SHA-256 `a8dba5d8a26c1d239932a34bce06d0e0e085bed27cdfc8355f2588eb5f3544e3`.
- Local APK: `dist/github-run-29964868198/CiYuanXia-v9.4-213-arm64-v8a.apk`, size `226821862` bytes, SHA-256 `b1bfcc0d585c5ed05dac109d831e01348498cbe8cb697ad53a3b79ef68b17308`.

## Known Risks
- Local Flutter/Dart/adb are unavailable in Termux; device install and UI smoke were not run locally.
- User should smoke-test Settings entries on device: battery optimization, storage access, floating file manager overlay, open-source licenses, CLI tools, terminal, and local proxy mapping persistence.

## Next Actions
- Install/smoke `9.4 / 213` on an Android device.
- Next fresh cloud build must use Android build greater than `213`.
