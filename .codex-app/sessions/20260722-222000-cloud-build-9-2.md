# 2026-07-22 Cloud Build 9.2

## Goal
- Continue the requested push/build after the CLI/terminal-only OpenClaw removal cleanup.
- Previous run `29961446821` failed in `Prepare Android local properties` while reading workflow build history.

## Repo Facts Read
- Project root: `/storage/emulated/0/ZeroTermux/开发/openclaw-termux-zh-5.5`.
- Active GitHub branch: `codex-terminal-ime-lag-fix`.
- Local checkout is detached; GitHub API push is preferred for this project.
- RootFS had already been rebuilt/published in earlier failed runs, so this retry used `rebuild_rootfs=false`.

## Changes Made
- Patched `.github/workflows/flutter-build.yml` so `detect_latest_workflow_build()` tolerates missing workflow names and failed log reads without aborting under `set -euo pipefail`.
- Committed locally as `88c98ee ci: tolerate unavailable workflow history` and pushed via GitHub API to remote `codex-terminal-ime-lag-fix` commit `0781146633a9d344b9cc75a72ad4652a0f4242a3`.

## Version And Artifacts
- Triggered workflow ID `307155792` with `publish_release=false` and `rebuild_rootfs=false`.
- Run `29961825999` succeeded.
- Selected Android build `211`, semantic `9.2.0`, display `9.2`.
- RootFS restore/verify passed from published `basic-resource`; RootFS rebuild/publish were skipped.
- Flutter analyze, arm64 APK build, PRoot native-library verification, artifact collection, and upload passed.
- Artifact: `ciyuanxia-apks`, ID `8546406371`, digest `sha256:f1f67352f36dfd06b4ea5d0337cc8783e6aea329bfa9c4edde2462956a08a89d`, size `213828801` bytes.

## Cloud Build
- GitHub Actions workflow ID `307155792` completed successfully as run `29961825999`.
- Remote head SHA: `0781146633a9d344b9cc75a72ad4652a0f4242a3`.
- Workflow still displays as `Build OpenClaw Apps` in GitHub metadata, despite source naming cleanup.

## Local Artifact
- Downloaded to `dist/github-run-29961825999/CiYuanXia-v9.2-211-arm64-v8a.apk`.
- APK size: `221488508` bytes.
- APK SHA-256: `24da1df5d9934dfb766fc3d7bb69d4bd580d7406068234920b01125c48aa9a75`.
- `unzip -t` passed.
- APK contains open-source notice assets, required arm64 PRoot libraries, and RootFS archive `assets/flutter_assets/assets/bootstrap/openclaw-rootfs-noble-arm64.tar.gz` at `199859592` bytes.

## Checks Run
- `npm test` passed 41/41.
- `npm run lint -- --no-warn-ignored` passed.
- `git diff --check` passed.
- `bash -n scripts/build-prebuilt-rootfs.sh scripts/prebuilt-rootfs-metadata.sh scripts/fetch-prebuilt-rootfs-asset.sh scripts/publish-prebuilt-rootfs-asset.sh scripts/build-apk.sh scripts/download-latest-artifact.sh` passed.
- `audit_github_actions.py --project .` passed with no warnings.
- `scan_app_secrets.py --project .` reported the known untracked `.tmp/rootfs-audit/.../certifi/cacert.pem` and `apiKey` field-name false positives; no committed secret was added.

## Known Risks
- Local Flutter/Dart/adb are unavailable; device smoke testing still needs an Android device.
- The workflow still appears in GitHub as `Build OpenClaw Apps`, so CI history lookup must continue to tolerate the legacy workflow name.

## Next Actions
- Install/smoke-test `dist/github-run-29961825999/CiYuanXia-v9.2-211-arm64-v8a.apk` on device.
- Next fresh cloud build must use Android build greater than `211`.
