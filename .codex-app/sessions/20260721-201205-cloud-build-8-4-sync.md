# 2026-07-21 20:12 UTC - Cloud Build 8.4 State Sync

## Goal
- User clarified that cloud build `8.4` has already completed and asked to synchronize project state.

## Repo Facts Read
- Existing app memory validated cleanly before edits.
- `gh auth status` showed an authenticated GitHub CLI session for account `shwiki1`; no token value was printed or stored.
- `gh run view 29740833706 --json databaseId,displayTitle,headBranch,headSha,conclusion,status,createdAt,updatedAt,url,event,workflowName` confirmed GitHub Actions run `29740833706` is `completed` with `success` on branch `codex-terminal-ime-lag-fix` at head SHA `ff7c6f6299ac2845a9af01e21b49fd0599fde196`.
- Existing memory already recorded artifact metadata for `8.4 / 203`: APK `CiYuanXia-v8.4-203-arm64-v8a.apk`, artifact `ciyuanxia-apks` ID `8460430702`, artifact digest `sha256:878cd528467a07825a95270148b7dbf7ef8805537ae3bac7bb4991875febdecd`, local APK SHA-256 `138036ffcfe0a740d8f2dc0785c592ded3fdec39cbeb2e8c7191cbdf6f7dbf36`.

## Changes Made
- Updated `.codex-app/state.md` so the current displayed artifact candidate is consistently `8.4 / 203`, latest build head is `ff7c6f6299ac2845a9af01e21b49fd0599fde196`, and the active device-smoke checklist uses `8.4 / 203` for both CodeBuddy and prior proxy/API-management regressions.
- Updated `.codex-app/build.md` to replace the old “8.4 expected next build” wording with the confirmed completed cloud run, and changed stale latest-candidate values from `8.0 / 199` to `8.4 / 203`.
- Updated `.codex-app/backlog.md` so the priority device-smoke task targets `8.4 / 203` instead of splitting proxy/API-management validation onto `8.3 / 202`.
- Added this handoff record.
- No business source code was changed.

## Checks Run
- `python3 /data/data/com.termux/files/home/.codex/skills/app-development-governor/scripts/validate_app_memory.py --project .` before edits: passed with no errors or warnings.
- `gh run view 29740833706 --json databaseId,displayTitle,headBranch,headSha,conclusion,status,createdAt,updatedAt,url,event,workflowName`: confirmed run success at `ff7c6f6299ac2845a9af01e21b49fd0599fde196`.

## Cloud Build
- No new cloud build was launched.
- Synchronized recorded status for existing cloud build run `29740833706`: success, workflow `Build OpenClaw Apps`, event `push`, branch `codex-terminal-ime-lag-fix`, head SHA `ff7c6f6299ac2845a9af01e21b49fd0599fde196`.

## Version And Artifacts
- Latest published GitHub Release remains `5.4 / 173`.
- Latest GitHub artifact candidate is confirmed as `8.4 / 203` from Actions run `29740833706`.
- Artifact: `ciyuanxia-apks`, ID `8460430702`, digest `sha256:878cd528467a07825a95270148b7dbf7ef8805537ae3bac7bb4991875febdecd`.
- Local APK: `dist/github-run-29740833706/CiYuanXia-v8.4-203-arm64-v8a.apk`, size `325175564` bytes, SHA-256 `138036ffcfe0a740d8f2dc0785c592ded3fdec39cbeb2e8c7191cbdf6f7dbf36`.
- Next fresh cloud build must use logical build greater than `203`.

## Known Risks
- Device smoke for `8.4 / 203` is still required.
- Local Flutter/Dart/adb tooling remains unavailable in this Termux environment, so APK compile/device verification remains cloud/device work.
- Workflow still lacks a `flutter test` gate and concurrency protection.

## Next Actions
- Device-smoke `8.4 / 203`: CodeBuddy through the local `api2py` relay, Codex tool-calling, local proxy health, API-management provider/model/protocol preservation, proxy browser layout/IME behavior, and Settings battery-optimization callback refresh.
- Do not launch another APK build unless requested; the next build number must be greater than `203`.
