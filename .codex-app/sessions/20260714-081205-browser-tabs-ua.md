# 2026-07-14 08:12 UTC - Browser Tabs And UA Fixes

## Goal

Fix the discovered browser automation issues locally without submitting a build: multi-tab support, UA switching, back/forward buttons, collapsed low-use tools, unified address/title field, and the desktop-UA mobile-page fallback.

## Repo Facts Read

- Read app governor instructions plus continuity, ecosystem-patterns, UI quality, testing matrix, and quality-gates references.
- Read `.codex-app/state.md`, `.codex-app/manifest.md`, `.codex-app/architecture.md`, `.codex-app/ui-system.md`, `.codex-app/build.md`, `.codex-app/backlog.md`, the latest session handoff, and relevant decisions.
- Verified browser tab/UA/tooling references in `terminal_browser_panel.dart`, `browser_automation_service.dart`, `cli_api_config_service.dart`, `cli_api_config_service_test.dart`, and `CHANGELOG.md`.

## Changes Made

- Added in-memory multi-tab browser support in `TerminalBrowserPanel`, including tab strip UI, per-tab WebView controllers, active tab switching, close/new tab actions, and state publication.
- Added browser UA switching for desktop/mobile via UI, bridge, MCP, and `browser-script ua <desktop|mobile>`.
- Restored header back/forward buttons, kept reload, unified the page title into the address field label, and moved scripts/inspector/recent actions/snapshot into the more menu.
- Added desktop UA request headers, Android wide-viewport/text-zoom configuration, and a best-effort desktop viewport hint after page load.
- Extended `BrowserAutomationService`, generated MCP/browser-script config, and tests for tab list/new/switch/close and UA actions.
- Corrected `CHANGELOG.md` so it only describes implemented browser tab/UA behavior.
- Updated `.codex-app/` memory files to reflect the new browser architecture and backlog.

## Checks Run

- `git diff --check`: passed.
- `npm test`: passed with 14 checks.
- `npm run lint -- --no-warn-ignored`: passed.
- Focused `rg` checks confirmed the new tab/UA tools and found no stale `重命名标签` / `tab_rename` references.
- `command -v dart`, `command -v flutter`, and `command -v kotlinc`: no local paths available.

## Cloud Build

- No cloud build was dispatched.
- No APK was packaged.
- No commit or push was made.

## Version And Artifacts

- Source version remains root `package.json` `2.0.50` and Flutter `flutter_app/pubspec.yaml` `2.0.50+140`.
- Latest recorded artifact remains GitHub Actions run `29283260131`, APK `CiYuanXia-v2.0.50-140-arm64-v8a.apk`.
- No new artifact was produced.

## Known Risks

- Local Termux cannot run Flutter analyze/test/format.
- Android device smoke is still needed for browser multi-tab, UA switching, and desktop-layout behavior.
- The desktop viewport hint is best-effort and some sites may still choose mobile layouts from server-side detection.

## Next Actions

- Device-smoke the browser tab/UA flow on Android.
- Run Flutter analyze/test in a Flutter SDK or GitHub Actions environment.
