# App Manifest

## Identity
- Name: 次元虾 / `ciyuanxia`
- Purpose: Chinese Android integration for OpenClaw Gateway with bundled/native PRoot runtime control.
- Android package: `com.agent.cyx`
- Dart package: `openclaw`
- Root npm package: `ciyuanxia`
- Stack: Flutter, Kotlin Android, PRoot Ubuntu RootFS, Node.js/OpenClaw runtime, npm CLI compatibility layer.
- Repository root: /storage/emulated/0/ZeroTermux/开发/openclaw-termux-zh-5.5

## Source Of Truth
- Verify facts against repository files before editing.
- Keep this directory current after meaningful changes.

## Repo Map
- App ecosystem/runtime: Flutter Android app shell bridged to Kotlin services through `MethodChannel` / `EventChannel`; Kotlin manages PRoot and foreground services; Ubuntu RootFS runs Node.js/OpenClaw.
- App entry points: Flutter `flutter_app/lib/main.dart`; app shell `flutter_app/lib/app.dart`; Android host `flutter_app/android/app/src/main/kotlin/com/nxg/openclawproot/MainActivity.kt`; npm CLI `bin/openclawx`.
- Core modules: `flutter_app/lib/screens/`, `providers/`, `services/`, `models/`, `widgets/`; native services in `flutter_app/android/app/src/main/kotlin/com/nxg/openclawproot/`; root npm CLI in `lib/`.
- Navigation/routing: Flutter `MaterialPageRoute` from splash/setup/dashboard screens; no declarative router package.
- State/data layer: Provider (`LocaleProvider`, `SetupProvider`, `GatewayProvider`, `NodeProvider`), service classes, `shared_preferences`, app files/RootFS files, WebSocket node frames.
- UI system: Material 3, Google Fonts Inter, `AppColors`, reusable status/progress/gateway/terminal widgets, responsive content clamps.
- Test/check commands: `npm test`; `npm run lint -- --no-warn-ignored`; in Flutter SDK environments run `cd flutter_app && flutter pub get && flutter analyze && flutter test`.
- Test matrix: local Node CLI self-test and ESLint currently pass; Flutter unit tests exist under `flutter_app/test/` but local SDK is unavailable in this Termux session; Android packaging/smoke is cloud/device work.
- Version source: root `package.json` and `flutter_app/pubspec.yaml`; Android `versionName`/`versionCode` come from `flutter_app/android/local.properties` generated by build scripts/workflow or Flutter build args.
- GitHub cloud build: `.github/workflows/flutter-build.yml` builds `CiYuanXia-v<version>-<versionCode>-arm64-v8a.apk`; release job can publish when on `main` or manual dispatch with `publish_release`.
- Backlog: .codex-app/backlog.md
